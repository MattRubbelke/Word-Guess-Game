        document.onkeyup = function(event) {
                var keyPress = event.key;
                for (var i = 0; i < word.length; i++) {
                        if (keyPress === answers.charAt(i)){
                                document.getElementById("emptyLetters").innerHTML=event.key;
                        };

                }
        }
}

 var chosenWord = document.getElementById("emptyLetters").innerHTML=answers[i=Math.floor(Math.random() * answers.length)];
                document.getElementById("guessesLeft").innerHTML="Number of Guesses Remaining: " + guessesLeft;
                };